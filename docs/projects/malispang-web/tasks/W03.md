Recommended Model
- PO/PM: GPT-6 Astra — Thinking High
- Codex Dev: GPT-6 Astra — Thinking High
- Reason: ตรวจ authority, data contracts และความเสี่ยงต่อสิทธิ์/จำนวน/เงิน
- Upgrade to: GPT-6 Astra / High เมื่อพบการเปลี่ยน policy, identity, accounting, concurrency หรือ release risk

สำหรับงานนี้ให้คุณเอกเลือก GPT-6 Astra และ Thinking High.
ChatGPT/PO-PM และ Codex Developer เป็นคนละการเลือกกับ AI model ของ LINE bot; ไม่เปลี่ยน bot model จากงานนี้.

# W03 — สร้าง web authentication, sessions และ scoped authorization

Parent: [MPW Epic #175](https://github.com/Eak-dev/malipang-shop/issues/175)
Status: BACKLOG / IMPLEMENTATION_GATED. No task dispatched by this brief.
GitHub task: [#179](https://github.com/Eak-dev/malipang-shop/issues/179).
Dependencies: [W02 / #178](https://github.com/Eak-dev/malipang-shop/issues/178).
Implementation additionally requires #58 CLOSED + verified LEGACY_FULLY_DECOMMISSIONED and explicit control transition.
Risk: High identity/security. Effort assumption: 4–7 focused person-days, not a delivery promise.

## Business goal
ล็อกอินคนเดิมและเห็นข้อมูลเฉพาะที่อนุญาต

## Allowed scope
LINE browser/LIFF verification, server sessions, web module grants, resource projections, pending identity reuse, revoke/logout/CSRF; additive schema only if approved

## Acceptance criteria
- [ ] wrong-channel/expired/spoofed token และ pending/inactive/revoked users เข้า business routes ไม่ได้
- [ ] self/branch/detail/list/export/evidence ต้องมี resolved scope; missing scope denied
- [ ] ไม่มี credential/token ใน frontend; identity mapping/relink uniqueness/audit ผ่าน concurrent tests

## Validation / UAT
- Focused verification: T01–T03; CSRF, cookie/origin, grant changes mid-session, old-role compatibility; synthetic D1 migration rerun
- UAT: iOS/Android LINE+browser login/cancel/relogin/pending/revoke ตาม approved test users
- Runtime candidate must pass npm run check, npx wrangler deploy --dry-run and required CI on exact head; schema changes add clean/old-shape/rerun/integrity/FK/compatibility checks.
- Docs-only tasks use link/evidence/scope verification; never run live fixtures or weaken gates to produce PASS.

## Data/config impact
session/grant schema และ TEST auth config หลังอนุมัติ; ไม่แก้ production binding

## Rollback / forward fix
disable approved web ingress/grants; preserve identity/history; revoke web sessions only

## Excluded scope
ไม่ทำ stock/finance writes, ไม่วาง ADMIN_TOKEN บน browser. Common exclusions: no Production data/messages/queue replay, no remote migration, no merge/deploy, no unrelated policy/secret/Legacy changes without exact named authorization.

## Entry points
src/access/authorization.ts; src/access/repository.ts; src/access/hr.ts; tests/identity-access.test.mjs
Read AGENTS.md, README.md, docs/07_ARCHITECTURE_AND_OPERATING_MODEL_TH.md and MPW PROJECT_CONTROL.md first, then [design reference](../03_PERMISSIONS.md) and only relevant diff/source. Paths for new modules are proposals, locate actual implementation before editing.

## Decision authority / handoff
Dev decides naming/helpers/local test structure within approved contract. Policy, identity mapping, data loss or deployment uncertainty → prepare concrete evidence-backed options for Owner; no silent assumption.
Deliver focused PR with base/head, before/after, AC→test evidence, skips/unknowns, migration/config impact, private-evidence references, UAT, rollback and exact next action.
Issue closes only after its full DoD and authorized closeout; a planning brief or CI PASS is not completion.
