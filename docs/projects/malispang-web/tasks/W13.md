Recommended Model
- PO/PM: GPT-6 Astra — Thinking High
- Codex Dev: GPT-6 Astra — Thinking High
- Reason: ตรวจ authority, data contracts และความเสี่ยงต่อสิทธิ์/จำนวน/เงิน
- Upgrade to: GPT-6 Astra / High เมื่อพบการเปลี่ยน policy, identity, accounting, concurrency หรือ release risk

สำหรับงานนี้ให้คุณเอกเลือก GPT-6 Astra และ Thinking High.
ChatGPT/PO-PM และ Codex Developer เป็นคนละการเลือกกับ AI model ของ LINE bot; ไม่เปลี่ยน bot model จากงานนี้.

# W13 — สร้าง operation receipts, audit, recovery และ TEST isolation

Parent: [MPW Epic #175](https://github.com/Eak-dev/malipang-shop/issues/175)
Status: BACKLOG / IMPLEMENTATION_GATED. No task dispatched by this brief.
GitHub task: [#189](https://github.com/Eak-dev/malipang-shop/issues/189).
Dependencies: [W03 / #179](https://github.com/Eak-dev/malipang-shop/issues/179).
Implementation additionally requires #58 CLOSED + verified LEGACY_FULLY_DECOMMISSIONED and explicit control transition.
Risk: High reliability/security. Effort assumption: 4–7 focused person-days, not a delivery promise.

## Business goal
ทุก web write กู้สถานะได้ และ notification failure ไม่ทำให้ธุรกรรมซ้ำ

## Allowed scope
shared idempotency/receipt/conditional commit design, durable side-effect intent, sanitized observability, private upload lifecycle, TEST isolation and operator runbooks

## Acceptance criteria
- [ ] business+audit+intent atomicity proven with failure injection; same/different payload rules correct
- [ ] TEST resources/secrets/AI allowlists isolated; no production fallback when config missing
- [ ] backup/recovery RPO/RTO measured against proposed targets and gaps explicit; no unsupported guarantee

## Validation / UAT
- Focused verification: T04/T05/T16/T19/T22; queue dispatch failure, lost response, stale lease, partial upload, revoked session during operation
- UAT: operator locates accepted-but-unnotified event, recovers only side effect, proves no second business commit
- Runtime candidate must pass npm run check, npx wrangler deploy --dry-run and required CI on exact head; schema changes add clean/old-shape/rerun/integrity/FK/compatibility checks.
- Docs-only tasks use link/evidence/scope verification; never run live fixtures or weaken gates to produce PASS.

## Data/config impact
shared receipt/intent tables or verified existing reuse; TEST infra only after authorization

## Rollback / forward fix
disable web writes; preserve receipts/outbox; do not delete unprocessed business evidence

## Excluded scope
ไม่ replay production queues, ไม่เพิ่ม timeout/retry กลบ root cause, ไม่เปลี่ยน retention/model. Common exclusions: no Production data/messages/queue replay, no remote migration, no merge/deploy, no unrelated policy/secret/Legacy changes without exact named authorization.

## Entry points
src/db/repositories.ts; src/line/attendance-notification.ts; src/admin/failed-job-reconciliation.ts; src/shared/retry.ts
Read AGENTS.md, README.md, docs/07_ARCHITECTURE_AND_OPERATING_MODEL_TH.md and MPW PROJECT_CONTROL.md first, then [design reference](../08_VALIDATION_OPERATIONS.md) and only relevant diff/source. Paths for new modules are proposals, locate actual implementation before editing.

## Decision authority / handoff
Dev decides naming/helpers/local test structure within approved contract. Policy, identity mapping, data loss or deployment uncertainty → prepare concrete evidence-backed options for Owner; no silent assumption.
Deliver focused PR with base/head, before/after, AC→test evidence, skips/unknowns, migration/config impact, private-evidence references, UAT, rollback and exact next action.
Issue closes only after its full DoD and authorized closeout; a planning brief or CI PASS is not completion.
