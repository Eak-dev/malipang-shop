Recommended Model
- PO/PM: GPT-6 Astra — Thinking High
- Codex Dev: GPT-6 Astra — Thinking High
- Reason: ตรวจ authority, data contracts และความเสี่ยงต่อสิทธิ์/จำนวน/เงิน
- Upgrade to: GPT-6 Astra / High เมื่อพบการเปลี่ยน policy, identity, accounting, concurrency หรือ release risk

สำหรับงานนี้ให้คุณเอกเลือก GPT-6 Astra และ Thinking High.
ChatGPT/PO-PM และ Codex Developer เป็นคนละการเลือกกับ AI model ของ LINE bot; ไม่เปลี่ยน bot model จากงานนี้.

# W14 — ซ้อม migration และพิสูจน์ reconciliation รายหมวด

Parent: [MPW Epic #175](https://github.com/Eak-dev/malipang-shop/issues/175)
Status: BACKLOG / IMPLEMENTATION_GATED. No task dispatched by this brief.
GitHub task: [#190](https://github.com/Eak-dev/malipang-shop/issues/190).
Dependencies: [W05 / #181](https://github.com/Eak-dev/malipang-shop/issues/181), [W06 / #182](https://github.com/Eak-dev/malipang-shop/issues/182), [W13 / #189](https://github.com/Eak-dev/malipang-shop/issues/189).
Implementation additionally requires #58 CLOSED + verified LEGACY_FULLY_DECOMMISSIONED and explicit control transition.
Risk: High migration/data. Effort assumption: 4–8 focused person-days, not a delivery promise.

## Business goal
พิสูจน์ว่าย้ายได้ครบและย้อนกลับได้โดยไม่ทิ้งรายการใหม่

## Allowed scope
per-module approved mappings, authorized snapshot/delta manifest, deterministic import/reuse identities, dry-run/quarantine, full counts/totals, old-writer fencing and rollback rehearsal

## Acceptance criteria
- [ ] no core mirror imported as new attendance/payroll/expense; source lineage and exclusions complete
- [ ] rerun idempotent; all scoped rows reconciled, no unexplained loss/duplicate/quantity/money delta
- [ ] post-cutover delta survives rollback; old writers cannot overwrite new state

## Validation / UAT
- Focused verification: T17–T19; missing/moved/duplicate source rows, interrupted import, concurrent edits across snapshot; restore integrity/FK
- UAT: rehearse Team scope first; rerun separately after W08–W12 for each expanded module
- Runtime candidate must pass npm run check, npx wrangler deploy --dry-run and required CI on exact head; schema changes add clean/old-shape/rerun/integrity/FK/compatibility checks.
- Docs-only tasks use link/evidence/scope verification; never run live fixtures or weaken gates to produce PASS.

## Data/config impact
synthetic/authorized TEST data only; no live cutover

## Rollback / forward fix
discard isolated rehearsal target only if authorized; preserve source evidence/manifests; prove forward-fix

## Excluded scope
ไม่ snapshot private data into GitHub, ไม่ mutate current Sheets/Production. Common exclusions: no Production data/messages/queue replay, no remote migration, no merge/deploy, no unrelated policy/secret/Legacy changes without exact named authorization.

## Entry points
approved W01 mapping; migrations; each implemented domain import/reconcile; existing Sheets recovery inventory
Read AGENTS.md, README.md, docs/07_ARCHITECTURE_AND_OPERATING_MODEL_TH.md and MPW PROJECT_CONTROL.md first, then [design reference](../07_MIGRATION_CUTOVER.md) and only relevant diff/source. Paths for new modules are proposals, locate actual implementation before editing.

## Decision authority / handoff
Dev decides naming/helpers/local test structure within approved contract. Policy, identity mapping, data loss or deployment uncertainty → prepare concrete evidence-backed options for Owner; no silent assumption.
Deliver focused PR with base/head, before/after, AC→test evidence, skips/unknowns, migration/config impact, private-evidence references, UAT, rollback and exact next action.
Issue closes only after its full DoD and authorized closeout; a planning brief or CI PASS is not completion.
